/* CREATE TABLE */
CREATE TABLE trains(
	id INT,
	name VARCHAR(100),
	description TEXT,
	distance_between_stop VARCHAR(100),
	max_speed VARCHAR(100),
	sharing_tracks boolean,
	grade_crossing boolean,
	train_frequency VARCHAR(100),
	amenities VARCHAR(500)
);
